import { ImgDirectivesDirective } from './img-directives.directive';

describe('ImgDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
