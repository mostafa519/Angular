import { ImgDirectiveDirective } from './img-directive.directive';

describe('ImgDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
