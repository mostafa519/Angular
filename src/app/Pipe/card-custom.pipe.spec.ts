import { CardCustomPipe } from './card-custom.pipe';

describe('CardCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new CardCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
