import { DiscountValuePipe } from './discount-value.pipe';

describe('DiscountValuePipe', () => {
  it('create an instance', () => {
    const pipe = new DiscountValuePipe();
    expect(pipe).toBeTruthy();
  });
});
